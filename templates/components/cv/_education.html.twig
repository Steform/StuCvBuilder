{% set currentLocale = currentLocale|default(app.request.locale|default('fr')) %}
{% set cv = cv|default({ payload: {} }) %}
<section id="education" class="cv-education cv-custom-section cv-custom-section--education cv-public-section py-5" aria-labelledby="cv-education-title">
    <div class="container">
        <header class="mb-4">
            <h2 id="cv-education-title" class="cv-education__title h3 mb-0">{{ 'cv.education.title'|trans({}, 'messages', currentLocale) }}</h2>
        </header>

        {% include 'components/cv/_education_timeline.html.twig' with {
            educationFilter: 'primary',
            cv: cv,
            currentLocale: currentLocale
        } only %}

        {% if cv.payload.educationHasSecondaryVisible|default(false) %}
            <p class="cv-education__see-more-wrap mt-4 mb-0">
                <a
                    href="{{ path('cv_education_full', app.request.query.has('format') ? { format: app.request.query.get('format') } : {}) }}"
                    class="cv-about-accent-btn--outline"
                    aria-label="{{ 'cv.education.see_more_aria_label'|trans({}, 'messages', currentLocale) }}"
                >
                    {{ 'cv.education.see_more'|trans({}, 'messages', currentLocale) }}
                </a>
            </p>
        {% endif %}
    </div>
</section>
